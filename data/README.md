# Data Policy

Raw industrial monitoring data are not included in this repository.

Directory purpose:

- `data/raw/`: local private inputs, ignored by git
- `data/processed/`: local derived datasets, ignored by git

To use this project, prepare local parquet files that match the schema in `docs/data_schema.md`.

